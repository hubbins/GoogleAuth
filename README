Sample using google auth with the Play Framework

Need to manually add google.conf to the conf folder containing the following two values
to complete the configuration:
google.clientId="CLIENTID.apps.googleusercontent.com"
google.signinScope="profile email"


Create an OAuth web application client id here and authorize the origin http://localhost:9000
https://console.developers.google.com/apis/credentials
